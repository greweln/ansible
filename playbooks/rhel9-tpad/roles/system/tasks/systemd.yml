- name: Copy the screen-lock unit
  copy:
    src: screen-lock.service
    dest: /etc/systemd/system/screen-lock.service
    mode: '0644'

- name: Reload systemd
  systemd:
    daemon_reload: yes

- name: Enable screen-lock service
  systemd:
    name: screen-lock.service
    enabled: yes
